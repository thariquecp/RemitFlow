name: fintech_app
description: "RemitFlow — Global Remittance & FX Exchange Platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  equatable: ^2.0.7
  get_it: ^9.0.0
  dio: ^5.7.0
  connectivity_plus: ^6.1.0
  go_router: ^14.8.0
  hive_ce: ^2.10.0
  hive_ce_flutter: ^2.3.4
  shared_preferences: ^2.3.0
  flutter_secure_storage: ^9.2.4
  local_auth: ^2.3.0
  flutter_animate: ^4.5.2
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  intl: ^0.20.2
  uuid: ^4.5.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hive_ce_generator: ^1.10.0
  build_runner: ^2.4.14

flutter:
  uses-material-design: true

  assets:
    - assets/
